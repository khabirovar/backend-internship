stages:
    - build war
#    - build docker
#    - public docker


build app:
    before_script:
        - mvn -v
    script:
        - mvn clean package -DskipTests -e
    artifacts:
        paths:
            - target/*.jar
        expire_in: 1 day
    stage: build war
